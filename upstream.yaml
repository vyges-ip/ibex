schema_version: "vyges-upstream-v1"
upstream:
  type: "standalone"
  repo: https://github.com/lowRISC/ibex
  branch: master
  commit: fc34556837c4c2e4665f6932296fb3b54cc9e857
import:
  rtl:
    - path: "rtl"
      map_to: "rtl"
  patches:
    - path: "vendor/patches"
      map_to: "vendor/patches"
  license:
    - path: "LICENSE"
  readme:
    - path: "README.md"
exclude:
# All other directories excluded by default
# Add more as needed
license:
  spdx: Apache-2.0
  file: LICENSE
metadata_locked: true
